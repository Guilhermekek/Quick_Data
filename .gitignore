# Ambiente local — não versionar
.venv/
.venv312/
__pycache__/
*.pyc
node_modules/
.idea/
.claude/

# Só o app (Front = app/ui, Back = app/*.py) vai pro GitHub. Tudo que
# não é código do app — documentação, fonte do design, scaffold antigo
# do frontend, scripts de build ligados a ele, e os arquivos originais
# do Quick Data (um deles com credencial de banco em texto plano, achado
# de segurança já documentado — nunca deve ir para o GitHub) — fica
# arquivado localmente aqui, fora do repositório.
_arquivo/
